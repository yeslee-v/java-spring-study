- 테스트할 때 `./gradlew test`도 같이 돌려볼 것

## 다시 발생한 DefaultCacheAwareContextLoaderDelegate
- github secrets에는 naver 환경변수 추가했으나
- actions 워크플로우가 ${NAVER_CLIENT_ID}와 ${NAVER_CLIENT_SECRET} 값을 전달하지 못함
- 저 두 환경변수는 `application.yml`에서 사용
  NaverBookSearchServiceImpl 빈 생성에서 프로퍼티 주입에 실패하고, 그 컨텍스트를 필요로 하는 테스트들이 전부
  IllegalStateException으로 실패

### 그래서 저게 뭔디?
