# Redis 설치
- MySQL 컨테이너와 독립적으로
```sh
docker pull redis:7-alpine

docker run -d --name rt-redis -p 6379:6379 redis:7-alpine

# test
redis-cli -h localhost -p 6379 ping
```

## AWS ElastiCache
- VPC 연결 시, private subnet 선택해야
- password(auth token)은 생성할 때만 만들 수 있다 → security 창에서 수정해서 못만든다