# auditing
날짜를 자동으로 붙여주는거

# Error creating bean with name 'jpaAuditingHandler': Cannot resolve reference to bean 'jpaMappingContext' while setting constructor argument
- 원인: jpaAuditingHandler는 readingTrackerApplication에 있었음
- 해결: jpaAuditingHandler 클래스를 config로 분리, ReadingTrackerApplication에서 제거한 @EnableJpaAuditing 붙임

# PrincipleServiceDetail
- 로그인한 유저를 db에서 조회해서 PrincipleDetail로 반환

# UserDetails와 OAuth2User
- 서비스에서는 소셜로그인을 구현했지만 테스트에서는 일반 로그인을 사용 → PrincipleDetail은 일반 로그인용 UserDetails를 상속받는다
- 테스트에서 소셜로그인으로 로그인하는건 테스트 목적에서 벗어나는거니까 빠르게 일반 로그인으로 구현

# public UserDetails loadUserByUsername(String username) throws UsernameNotFoundException
- username: 로그인 창에 입력하는 아이디
- 지금까지 닉네임, 유저 이름으로 생각하고 썼는데 명칭이 헷갈리게 정의되어있음..
- 변수명의 중요성을 깨닫는다

# 서비스단에는 중단점 찍어도 mock 데이터라 그런지 디버깅할 때 안들어감
- 진짜 service가 아닌 가짜 껍데기 service로 테스트 → service 코드 실행하지 않는다
- mock은 필터링을 못한다…..
- service 동작 = given으로 주입

# given을 사용할 때 인자는 전부 matcher로 통일
- raw냐 아님 eq()로 감싼 matcher 둘 중 하나를 선택해서 테스트 전부를 통일해야
- matcher로 통일하는 것이 보편적 → 